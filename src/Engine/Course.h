/*class Course
{
private:
    Terrain* terrain;
    std::vector<Actor> actors;
    float gravity;
public:
    Course();
    Course(Terrain* terrain);
    std::vector<Actor> GetActors();
    void AddActor(Actor actor);
    void RemoveActor(Actor actor);
}*/